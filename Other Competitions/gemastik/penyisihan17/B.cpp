#include<bits/stdc++.h>
using namespace std;

using ll = long long;
using ld = long double;

void solve() {
  int a, b, k;
  cin >> a >> b >> k;
  if(b > (k - 1) * a) {
    cout << "mustahil" << '\n';
    return;
  }
  int cur = 0;
  while(cur < a + b) {
    if(cur % )
  }
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);

  int TC = 1;
  cin >> TC;
  for(int i = 1; i <= TC; i++) {
    solve();
  }
  
  return 0;
}