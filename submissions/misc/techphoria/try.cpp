#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back
#define ff first
#define ss second
#define forn(i,a,b) for(int i = (int) a; i < (int) b; i++)
#define forn(i,n) for(int i = 0; i < n; i++)
#define fill(a,b) memset(a, (b), sizeof(a))
#define pii pair<int, int>
#define pll pair<long long, long long>

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);	
	cout.tie(NULL);

	int x = (1>0) ? 2 : 3;
	cout<<x<<"\n";

	//try memset, cuma bisa 0 dan -1
	int a[4];
	forn(i,4) cout<<a[i]<<"\n";
	fill(a, 0);
	forn(i,4) cout<<a[i]<<"\n";

	//try map

	//cek inisial value of a map

	//iterate through a map

	//try set

	//iterate through a set




	
}