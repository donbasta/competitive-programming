#include<bits/stdc++.h>
using namespace std;

using ll = long long;
using ld = long double;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);

  vector<int> ve;
  sort(ve.begin(), ve.end());
  for(auto i : ve) 
    cerr << i << '\n';
  cerr << "TES";
  
  return 0;
}