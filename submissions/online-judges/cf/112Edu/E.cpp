#include<bits/stdc++.h>
using namespace std;

using ll = long long;
using ld = long double;

int n, m;

const int M = 1e6;
pair<int, int> pa[M + 10];

ll dp(int a) {
    if (a >= m)
}

void solve() {
    cin >> n >> m;
    vector<pair<int, int>> 
    for (int i = 1; i <= n; i++) {
        cin >> l >> r > w;
        pa[l].emplace_back(r, w);
    }
    cout << dp(1);
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int TC = 1;
    for(int i = 1; i <= TC; i++) {
        solve();
    }
  
    return 0;
}