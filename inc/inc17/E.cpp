#include<bits/stdc++.h>
using namespace std;

using ll = long long;
using ld = long double;

struct Node {
  
}

struct Segtree {

}

inline void solve() {
  cin >> n >> q >> st;
  cin >> q;
  while(q--) {
    int t, l, r;
    cin >> t >> l >> r;
    if(t == 1) {

    } else {

    }
  }

}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);

  int TC = 1;
  for(int i = 1; i <= TC; i++) {
    solve();
  }
  
  return 0;
}